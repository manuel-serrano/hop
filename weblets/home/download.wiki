== Getting Hop ==

,(<DIV> :class "os-download" :align 'center
  (<DIV> :class "download-header" "Stable versions")
  (<TABLE> :class "os-download os-download-stable"
     (<COLGROUP> 
        (<COL>)
        (<COL> :width "20%")
        (<COL>)
        (<COL> :width "20%")
        (<COL>)
        (<COL> :width "20%"))
     (<TR> 
        (<TD> :rowspan 2 :class "type"
           (<IMG> :inline #t
             :src (make-file-path (home-dir) "etc" "icons" "linux.png")))
        (<TH> "Linux")
        (<TD> :rowspan 2 :class "type"
           (<IMG> :inline #t
             :src (make-file-path (home-dir) "etc" "icons" "maemo_install.png")))
        (<TH> "Maemo")
        (<TD> :rowspan 2 :class "type"
           (<IMG> :inline #t
             :src (make-file-path (home-dir) "etc" "icons" "macos.png")))
        (<TH> "MacOS X")
        (<TD> :rowspan 2 :class "type"
           (<IMG> :inline #t
             :src (make-file-path (home-dir) "etc" "icons" "windows.png")))
        (<TH> "Windows"))
     (<TR> 
         (<TD> (<A> :href *stable-tar-url* *stable-tar-gz*))
         (<TD> (<A> :href *maemo-fremantle-url* "Fremantle"))
         (<TD> (<A> :href *stable-dmg-url* (basename *stable-dmg*)))
         (<TD> (<A> :href *stable-jar-url* (basename *stable-jar*))))))

,(<DIV> :class "os-download" :align 'center
  (<DIV> :class "download-header" "Unstable versions")
  (<TABLE> :class "os-download os-download-unstable"
     (<COLGROUP> 
        (<COL>)
        (<COL> :width "50%")
        (<COL>)
        (<COL> :width "50%"))
     (<TR> 
        (<TD> :rowspan 2 :class "type"
           (<IMG> :inline #t
             :src (make-file-path (home-dir) "etc" "icons" "package.png")))
        (<TH> "Source")
        (<TD> :rowspan 2 :class "type"
           (<IMG> :inline #t
             :src (make-file-path (home-dir) "etc" "icons" "java.png")))
        (<TH> "Pre-compiled JVM"))
     (<TR> 
         (<TD> (<A> :href *devel-tar-url* *devel-tar-gz*))
         (<TD> (<A> :href *devel-jar-url* *devel-jar*)))))


,(<TABLE>
   :class "download"
   (<TR> (<TD> "&nbsp;"))
   (<TR> (<TH> [Older versions may be download by ,(<SPAN> :class "old" "ftp") at:]
         (<A> :href (home-ftp-hop-url) (home-ftp-hop-url))))
   (<TR> (<TD> "&nbsp;"))
   (<TR> (<TD> 
[Linux users ,(<B> "should") prefer the native version built
from the source code to the pre-compiled JVM version because it gives 
much better performance. On Linux, the JVM version should only be considered
for quick Hop experiments.])))

~~ HOP is shipped with some **Linux distributions** on which it can be
used without downloading and installing the source code manually:

  * ,(<IMG> :inline #t :src (make-file-path (home-dir) "etc" "icons" "arch-16x16.png")) [[http://aur.archlinux.org|Arch Linux]]
  * ,(<IMG> :inline #t :src (make-file-path (home-dir) "etc" "icons" "gentoo-16x16.png")) [[http://www.gentoo.org|Gentoo]]


~~ We provide some pre-compiled packages for non-standard architectures:

  * ,(<IMG> :inline #t :src (make-file-path (home-dir) "etc" "icons" "maemo-16x16.png")) [[http://maemo.org|Maemo]]

,(<DIV> :class "os-download" :align 'center
  (<DIV> :class "download-header" "Stable Maemo")
  (<TABLE> :class "os-download os-download-maemo"
     (<COLGROUP> 
        (<COL>)
        (<COL> :width "50%")
        (<COL>)
        (<COL> :width "50%"))
     (<TR> 
        (<TD> :rowspan 2 :class "type"
           (<IMG> :inline #t
             :src (make-file-path (home-dir) "etc" "icons" "maemo_install.png")))
        (<TH> "OS2008")
        (<TD> :rowspan 2 :class "type"
           (<IMG> :inline #t
             :src (make-file-path (home-dir) "etc" "icons" "maemo_install.png")))
        (<TH> "Maemo5"))
     (<TR> 
         (<TD> (<A> :href *maemo-chinook-free-url* "Chinook"))
         (<TD> (<A> :href *maemo-fremantle-free-url* "Fremantle")))))

,(<DIV> :class "os-download" :align 'center
  (<DIV> :class "download-header" "Unstable Maemo")
  (<TABLE> :class "os-download os-download-unstable"
     (<COLGROUP> 
        (<COL>)
        (<COL> :width "50%")
        (<COL>)
        (<COL> :width "50%"))
     (<TR> 
        (<TD> :rowspan 2 :class "type"
           (<IMG> :inline #t
             :src (make-file-path (home-dir) "etc" "icons" "maemo_install.png")))
        (<TH> "OS2008")
        (<TD> :rowspan 2 :class "type"
           (<IMG> :inline #t
             :src (make-file-path (home-dir) "etc" "icons" "maemo_install.png")))
        (<TH> "Maemo5"))
     (<TR> 
         (<TD> (<A> :href *maemo-chinook-unstable-url* "Chinook"))
         (<TD> (<A> :href *maemo-fremantle-unstable-url* "Fremantle")))))


== Getting Bigloo ==

In order to install HOP from the source (for significantly improving
performance), the ,(<A> :href (home-bigloo-url) "Bigloo") compiler has
first to be installed. Bigloo version **3.3a** or higher is required for
compiling HOP.

~~ Bigloo versions may be downloaded
from: ,(<A> :href (home-ftp-bigloo-url) (home-ftp-bigloo-url)).

== Browsing the code ==

The HOP source code may be browsed 
,(<A> :href (home-source-url) "here").

== License ==

<include>license.wiki</include>
