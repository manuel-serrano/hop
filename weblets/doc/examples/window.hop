;; Outer windows
(define-service (window/iota)
   (<HTML>
      (<TABLE>
	 (map (lambda (i) (<TR> (map <TD> (iota 10 i))))
	      (iota 10)))))

(<HTML>
   (<HEAD> :include "hop-iwindow")
   (<BODY>
      (<BUTTON>
	 :onclick ~(window-open ($window/iota) "iota")
	 "clickme")))
