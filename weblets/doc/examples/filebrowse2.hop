;; an example of INPUT :type url markup
(<HTML>
    (<HEAD>
       :include "hop-file")
    (<BODY>
       (<DIV> "Hit the [tab] key to complete the current prefix. This
can be used in WebDAV urls too.")
       (<INPUT>
	  :type 'url
	  :value "/tmp"
	  :size 80
	  :onchange ~(alert "FILE=" this.value))))
