;; A simple //tooltip//
(<HTML>
   (<HEAD> :include "hop-tooltip")
   (<BODY>
      (let ((tp (<TOOLTIP> (<TABLE>
			      (<TR> (<TH> :colspan 2 "A tooltip"))
			      (<TR> (<TD> 1) (<TD> 2))
			      (<TR> (<TD> "one") (<TD> "two"))))))
	 (list
	  tp
	  (<SPAN>
	     "Come with me"
	     :onmouseover ~(tooltip-show event $tp (- (event-mouse-x event) 10) (- (event-mouse-y event) 100))
	     :onmouseout ~(tooltip-hide))))))
