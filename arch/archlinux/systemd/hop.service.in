[Unit]
Description=Hop Web Broker

[Service]
Type=forking
EnvironmentFile=/etc/conf.d/hop
ExecStart=/usr/sbin/hop -p ${PORT} --accept-kill --user ${DAEMONUSER}
ExecStop=/usr/sbin/hop -p ${PORT} --kill --user ${DAEMONUSER}

[Install]
WantedBy=multi-user.target
