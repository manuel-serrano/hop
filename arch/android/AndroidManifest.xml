<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="fr.inria.hop"
      android:versionCode="2"
      android:versionName="2.1.0">
    <application android:label="@string/app_name"
                 android:icon="@drawable/icon">
        <activity android:name="fr.inria.hop.hop"
                  android:launchMode="singleInstance"
                  android:label="@string/app_name">
        </activity>
        <activity android:name="fr.inria.hop.Term"
                  android:label="@string/app_name"
                  android:configChanges="keyboardHidden|orientation">
            <!-- Using this^^^^^^^^^^^^ attribute should be avoided and used
               only as a last-resort. Please read Handling Runtime Changes for
               more information about how to properly handle a restart due to a
               configuration change. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
