{
   "tags": [ "websocket" ],
   "description": "Hop.js websocket native support",
   "doc": "Example showing how to use client and server websockets",
   "files": [ "wsserver.js", "wsclient.js" ],
   "level": 3,
   "commands": [ "hop --no-color --no-zeroconf -p $(PORT) --rc-file $(RCFILE) wsserver.js",
		 "hop --no-color --no-zeroconf -p $(PORT) --rc-file $(RCFILE) -- wsclient.js $(PREPORT)" ],
   "service": false
}
