{
   "tags": ["service", "security" ],
   "description": "Service authentication",
   "doc": "This example shows how to implement custom access rules.",
   "level": 1.1,
   "files": [ "authentication2.js", "remote.js", "passwd.hop" ],
   "commands": [ "hop --no-color --no-zeroconf -p $(PORT) --rc-file $(RCFILE) -- authentication2.js $(NEXTPORT)",
		 "hop --no-color --no-zeroconf -p $(PORT) --rc-file passwd.hop remote.js" ],
   "service": false
}
